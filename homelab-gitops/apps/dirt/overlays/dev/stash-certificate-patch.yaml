apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: stash-tls
spec:
  secretName: stash-dev-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - stash-dev.thundertest.org

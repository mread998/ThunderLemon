apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: whisparr-tls
spec:
  secretName: whisparr-dev-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - whisparr-dev.thundertest.org

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n8n
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  tls:
    - hosts:
        - n8n-dev.thundertest.org
      secretName: n8n-dev-tls
  rules:
    - host: n8n-dev.thundertest.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n8n
                port:
                  number: 80

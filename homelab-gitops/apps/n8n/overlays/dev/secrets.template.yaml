# TEMPLATE ONLY - DO NOT COMMIT WITH REAL VALUES
# Use this to generate dev sealed secrets:
#
#   kubeseal --namespace n8n-dev --name n8n-secrets \
#     --secret-file secrets.template.yaml \
#     --sealed-secret-file sealedsecrets.yaml \
#     --cert <path-to-pub-cert>
#
apiVersion: v1
kind: Secret
metadata:
  name: n8n-secrets
  namespace: n8n-dev
type: Opaque
stringData:
  N8N_ENCRYPTION_KEY: "changeme"
  N8N_BASIC_AUTH_ACTIVE: "true"
  N8N_BASIC_AUTH_USER: "admin"
  N8N_BASIC_AUTH_PASSWORD: "changeme"
  POSTGRES_DB: "n8n"
  POSTGRES_USER: "n8n"
  POSTGRES_PASSWORD: "changeme"

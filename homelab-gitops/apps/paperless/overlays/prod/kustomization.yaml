apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paperless

bases:
  - ../../base

patches:
  - path: replicas.yaml
    target:
      kind: Deployment
      name: paperless

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: paperless

resources:
  - namespace.yaml
  - pvc.yaml
  - sealedsecrets.yaml
  - configmap.yaml
  - postgres.yaml
  - redis.yaml
  - deployment.yaml
  - service.yaml
  - certificate.yaml
  - ingress.yaml

commonLabels:
  app.kubernetes.io/name: paperless

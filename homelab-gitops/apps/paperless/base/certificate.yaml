apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: paperless-tls
  namespace: paperless
spec:
  secretName: paperless-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - paperless.thundertest.org

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: navidrome-tls
  namespace: media
spec:
  secretName: navidrome-tls
  issuerRef:
    name: letsencrypt-cloudflare-prod
    kind: ClusterIssuer
  dnsNames:
    - navidrome.thundertest.org

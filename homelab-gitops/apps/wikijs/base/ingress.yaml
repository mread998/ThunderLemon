apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wikijs
  namespace: wikijs
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - wikijs.thundertest.org
      secretName: wikijs-tls
  rules:
    - host: wikijs.thundertest.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wikijs
                port:
                  number: 80

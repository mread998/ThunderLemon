apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wikijs-postgres-data
  namespace: wikijs
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

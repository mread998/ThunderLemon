apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: awx-tls
spec:
  secretName: awx-dev-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - awx-dev.thundertest.org

apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: homepage-tls
spec:
  secretName: homepage-dev-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - homepage-dev.thundertest.com

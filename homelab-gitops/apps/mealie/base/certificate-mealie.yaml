apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: mealie-tls
  namespace: mealie
spec:
  secretName: mealie-tls
  issuerRef:
    name: selfsigned
    kind: ClusterIssuer
  dnsNames:
    - mealie.thundertest.org
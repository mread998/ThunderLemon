apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mealie
spec:
  tls:
    - hosts:
        - mealie-dev.thunderchorme.com
      secretName: mealie-dev-tls
  rules:
    - host: mealie-dev.thunderchorme.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mealie
                port:
                  number: 80

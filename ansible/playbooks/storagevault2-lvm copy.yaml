---
- name: Configure storage LVM
  hosts: all
  become: true
  gather_facts: true

  vars:
    vg_storage_disks:
      - /dev/sdb
      - /dev/sdc
    vg_vault_disks:
      - /dev/sdd
      - /dev/sde
    mount_point: /srv/vault
    vg_storage: vg_storage
    vg_vault: vg_vault
    lv_name: vault_lv
    lv_path: "/dev/{{ vg_vault }}/{{ lv_name }}"
    fs_type: xfs

  tasks:
    - name: Ensure required packages are installed
      ansible.builtin.package:
        name:
          - lvm2
          - xfsprogs
        state: present

    - name: Ensure physical volumes exist for vg_storage
      community.general.lvm_pv:
        pvs: "{{ vg_storage_disks }}"
        state: present

    - name: Ensure vg_storage volume group
      community.general.lvm_vg:
        vg: "{{ vg_storage }}"
        pvs: "{{ vg_storage_disks }}"
        state: present

    - name: Ensure physical volumes exist for vg_vault
      community.general.lvm_pv:
        pvs: "{{ vg_vault_disks }}"
        state: present

    - name: Ensure vg_vault volume group
      community.general.lvm_vg:
        vg: "{{ vg_vault }}"
        pvs: "{{ vg_vault_disks }}"
        state: present

    - name: Create logical volume for vault storage
      community.general.lvm_lv:
        vg: "{{ vg_vault }}"
        lv: "{{ lv_name }}"
        size: 100%FREE
        state: present

    - name: Detect filesystem type on logical volume
      ansible.builtin.command: "lsblk -no FSTYPE {{ lv_path }}"
      register: lv_fstype
      changed_when: false
      failed_when: false

    - name: Create filesystem on logical volume if missing
      ansible.builtin.filesystem:
        fstype: "{{ fs_type }}"
        dev: "{{ lv_path }}"
      when: (lv_fstype.stdout | trim) == ""

    - name: Ensure mount directory exists
      ansible.builtin.file:
        path: "{{ mount_point }}"
        state: directory
        owner: root
        group: root
        mode: "0755"

    - name: Mount vault volume and persist in fstab
      ansible.builtin.mount:
        path: "{{ mount_point }}"
        src: "{{ lv_path }}"
        fstype: "{{ fs_type }}"
        opts: defaults
        state: mounted